# End-to-end (ovoscope) test dependencies.
ovoscope>=1.5.0
# Adapt pipeline for the IP / last-digits intents. Prerelease-floor pin: the
# stable ovos-adapt-parser still caps ovos-workshop, so pin the workshop-free
# prerelease line as the minimum.
ovos-adapt-parser>=1.3.3a1
